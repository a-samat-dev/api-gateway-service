server:
  port: ${SERVER_PORT:8080}
spring:
  application:
    name: api-gateway-service
jwt:
  secret: ${JWT_SECRET:jwtSecret}
service-mapping:
  user-service: ${USER_SERVICE:http://localhost:8082}
  patient-service: ${PATIENT_SERVICE:http://localhost:8083}
  schedule-service: ${SCHEDULE_SERVICE:http://localhost:8083}